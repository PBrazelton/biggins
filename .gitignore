# build output
dist/
# generated types
.astro/
# dependencies
node_modules/
# logs
npm-debug.log*
# environment
.env
.env.production
# macOS
.DS_Store
# original Claude Design export bundle (reference only — kept locally, not tracked)
_source/

# macOS text clippings (may contain pasted secrets)
*.textClipping
