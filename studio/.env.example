# Copy to studio/.env and fill in. These are Studio-side (browser) vars, so they
# MUST be prefixed SANITY_STUDIO_. projectId is public (safe to expose).
SANITY_STUDIO_PROJECT_ID=your_project_id
SANITY_STUDIO_DATASET=production
